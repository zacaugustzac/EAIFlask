version: 0.2

phases:
  install:
    commands:
      - bundle install
  pre_build:
    commands:
     - echo Executing Pre Build Phase
  build:
    commands:
     - echo Executing Pre Build Finally
  post_build:
    commands:
     - echo Executing Post Build Phase

    